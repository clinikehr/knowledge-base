---
title: Run quality control
description: "Record daily control values, read the Levey-Jennings chart, and act on the Westgard verdict the lab gets back the moment a run is saved."
icon: /images/icons/chart-line.svg
---

import { Availability } from '/snippets/availability.mdx';
import { Path } from '/snippets/path.mdx';
import { TaskHeader } from '/snippets/task-header.mdx';
import { StillStuck } from '/snippets/support.mdx';

<Availability editions={['lims']} plans="Professional Lab and above" roles="Owner, Manager and everyone else who reaches Lab Station, bench scientists included. Radiology-only roles do not." note="No permission gate — the plan is the only gate on this page" />

Record the day's control values against each material and ClinikEHR evaluates every run for you. The page says so in its own subtitle: "Record daily controls per material; every run is evaluated against Westgard rules (1-3s, 2-2s, R-4s, 4-1s, 10x — 1-2s warns) the moment it's saved." You enter a number; you get back a verdict and a charted point.

**The verdict is computed when you save.** Nobody on the screen decides whether a run passed — you cannot mark a rejected run as acceptable, and you do not apply the rules yourself.

<Path steps={['Sidebar', 'Quality Control']} />

<TaskHeader before="A control material, with the target mean and SD from its package insert or your lot validation" time="Under a minute per run" after="A charted run carrying an in-control, warning or rejection verdict" />

## What you're looking at

The page is one screen in three stacked parts.

| Part | What it holds |
| --- | --- |
| The material picker | **Select control material**, listing each control as `\{code\} — \{name\} (lot \{lot\})` with **Target \{mean\} · SD \{sd\}** beside it, plus **Add control material** and a **Refresh** button |
| The **Levey-Jennings** chart | The last 30 runs for the chosen material, plotted in sequence over reference bands at the mean and ±1, ±2 and ±3 SD |
| The run form and history | **Measured value**, **Technician** and **Record run**, above a table of **When**, **Value**, **z**, **Verdict**, **Rules** and **Technician** |

Every historical run carries a badge — **In control**, **Warning (1-2s)** or **Rejection** — and the chart colours each point to match, with its value and any violated rules on hover.

**The empty state.** A brand-new lab has no materials, so the page reads **No control materials yet. Add your first control (analyte, target mean and SD from the package insert or your lot validation) to start charting.** A material that exists but has never been run reads **No runs recorded yet for this control — record the first run below.** Below Professional Lab you see none of it — just a locked card.

## Set up a control material

Do this once per analyte and level.

<Steps>
  <Step title="Open the dialog">
    Select **Add control material**. It opens explaining where the numbers come from: "Target mean and SD come from the package insert or your lot validation — Westgard rules evaluate every run against them."
  </Step>
  <Step title="Identify the control">
    Enter an **Analyte code** (for example `GLU`), a **Material name** (for example `Glucose Control`), and pick the **Level** — **Level 1 (normal)**, **Level 2 (abnormal)** or **Level 3 (high)**. Add the **Lot number** so the picker can tell two lots of the same control apart.
  </Step>
  <Step title="Enter the targets">
    Fill **Target mean**, **Target SD** and **Unit** (for example `mg/dL`). Take all three from the package insert or from your own lot validation, never from memory. The SD must be greater than zero — it is what every z-score is measured in.
  </Step>
  <Step title="Save">
    Select **Add material**. The toast reads **Control material added** and the control joins the picker.
  </Step>
</Steps>

## Record a run

<Steps>
  <Step title="Choose the control">
    Pick it from **Select control material**. The **Levey-Jennings** chart redraws for that material.
  </Step>
  <Step title="Enter the measured value">
    Type the reading into **Measured value**. The field's placeholder is the target mean, so a value far from it is visible before you save.
  </Step>
  <Step title="Name who ran it">
    Enter the person in **Technician**. It is the column a reviewer reads first when a run fails.
  </Step>
  <Step title="Record it">
    Select **Record run**. The run is evaluated against every Westgard rule as it saves, and comes back with one of three verdicts.
  </Step>
</Steps>

## The three verdicts

| You see | Means |
| --- | --- |
| **In control (z \{z\})** | Nothing tripped. Report patients. |
| **1-2s warning (z \{z\}) — inspect before accepting the run.** | A warning, not a rejection. Look before you accept. |
| **Run REJECTED — \{violations\} (z \{z\}). Stop and troubleshoot before reporting patients.** | One or more rejection rules tripped. |

<Warning>
  A rejected run means **stop**. Do not report patient results on that analyte until you have found and fixed the cause and a fresh control passes. The verdict names the rules it broke so you know what you are looking for.
</Warning>

## What each rule is telling you

| Rule | One control run in plain terms | Usually |
| --- | --- | --- |
| **1-2s** | One value fell between 2 and 3 SD from the mean. | A warning to inspect, nothing more |
| **1-3s** | One value fell beyond 3 SD. | Random error — a bubble, a bad pipette, a mis-made control |
| **2-2s** | Two runs in a row fell beyond the same 2 SD limit. | Systematic error — calibration or reagent |
| **R-4s** | Two consecutive runs sat on opposite sides, 4 SD apart. | Imprecision — the method has got noisy |
| **4-1s** | Four runs in a row fell beyond the same 1 SD limit. | A shift — something changed and stayed changed |
| **10x** | Ten runs in a row sat on the same side of the mean. | Drift or bias — often a new reagent or calibrator lot |

## Who can do this

Your **plan** decides whether **Quality Control** exists; **role** decides whether it appears in your sidebar. There is no permission gate on this page at all.

| Action | Who by default | Governed by |
| --- | --- | --- |
| Open **Quality Control** | Everyone who reaches **Lab Station** — Owner, Manager, bench scientists and the business roles | Plan only — the quality-control feature |
| Add a control material | The same people | Plan only — no permission |
| Record a run and read the verdict | The same people | Plan only — no permission |
| Act on a rejection at the bench — recalibrate, re-run patients | Anyone on the **Own bench** tier and above | Permission — **Enter results at the bench** |
| Release patient results once controls pass | Anyone on the **Whole lab** or **All sites** tier | Permission — **Verify and authorise results** |

**Review QC runs is not consulted here.** The permission exists in the Diagnostics catalogue and the permissions sheet will let you grant or withhold it, but nothing on this page or in the **Quality Control** report reads it — the paid feature is the whole gate. Granting it changes nothing; withholding it locks nobody out. Your local procedure decides who records controls, not the software.

**QC Officer is review-only by design** in every other respect: the role inspects work rather than altering it, and holds nothing that lets it touch a patient result.

The three access tiers decide the last two rows. **Own bench** grants result entry only; **Whole lab** adds accessioning, rejection, verification and QC review at that site; **All sites** grants everything, across every site. Managing the lab is not the same as authorising a result — a **Lab Manager** does not get entry, verification or amendment by virtue of managing.

Two behaviours surprise people:

- **Enforcement is opt-in, per person.** Somebody never saved in the permissions sheet is unrestricted, and the sheet warns **"Saving starts enforcing"**.
- **A denied action is recorded; an allowed one is not.** Where a QC figure is not yours to see, it renders hidden or as a dash — never as a zero.

See [Permissions](/platform/team/permissions).

## Check it worked

- The toast names the verdict and the z-score.
- A new point appears on the **Levey-Jennings** chart, coloured for its verdict.
- The run heads the table below the chart with its **Verdict** badge and the technician's name.

## Common issues

<AccordionGroup>
  <Accordion title="The page shows a locked card instead of the chart">
    You are on Starter Lab. The card reads **QC management is a Professional feature — Daily controls with Levey-Jennings charts and automatic Westgard flagging. Included from the Professional Lab plan.** See [Change your plan](/platform/plans/change-plan).
  </Accordion>

  <Accordion title="The material would not save">
    The toast reads **Analyte, name, target mean and a positive SD are required**. An SD of zero or blank is the usual cause.
  </Accordion>

  <Accordion title="Every run comes back rejected on a new lot">
    The targets belong to the old lot. Add the new lot as its own control material with its own mean and SD. New reagent lots are also quarantined until validated — see [Reagents and consumables](/lims/inventory/reagents).
  </Accordion>

  <Accordion title="Record run refuses with “Enter the measured control value”">
    Either **Measured value** is blank, or no material is picked in **Select control material**. The message covers both, so check the picker before you retype the number.
  </Accordion>

  <Accordion title="The chart says there are no runs">
    It reads **No runs recorded yet for this control — record the first run below.** The material exists but has no history.
  </Accordion>

  <Accordion title="I can open QC but cannot fix anything on the bench">
    That is deliberate — see **Who can do this** above. Entering and authorising results are separate grants, see [Enter and verify results](/lims/lab/results).
  </Accordion>

  <Accordion title="Quality Control is not in the sidebar at all">
    Quality control management is still rolling out and reaches labs progressively on the Professional Lab plan. If your plan includes it and the entry is absent, [contact support](/platform/help/contact-support).
  </Accordion>
</AccordionGroup>

## FAQ

<AccordionGroup>
  <Accordion title="Can I override a rejected run and report the patients anyway?">
    Not on this screen. The verdict is computed when the run is saved and there is no accept-anyway control. Fix the cause, run a fresh control, and report once that one passes.
  </Accordion>

  <Accordion title="How many levels should I run?">
    Whatever your method and your accreditation body require — usually two, a normal and an abnormal. Each level is its own control material with its own mean and SD, so **Level 1 (normal)** and **Level 2 (abnormal)** chart separately.
  </Accordion>

  <Accordion title="Where do the target mean and SD come from?">
    The control's package insert, or your own lot validation once you have enough runs to establish in-house values. They are not calculated for you, and they are what every z-score and every rule is measured against.
  </Accordion>

  <Accordion title="Does QC stop results being released automatically?">
    No. QC is the evidence, not a lock — releasing a report is a separate, deliberate act by someone holding **Verify and authorise results**. Reading QC before signing out is a habit your procedures have to enforce.
  </Accordion>

  <Accordion title="Why can a QC Officer see turnaround time as well?">
    Both are oversight of how the lab performs rather than changes to a patient record, so they travel together — see [Turnaround time](/lims/lab/turnaround-time).
  </Accordion>
</AccordionGroup>

<StillStuck topic="quality control" />

{/* source of truth: components/clinic/dashboard/lab-station/qc/qc-content.tsx, lib/lims-pricing-config.ts, lib/edition-staff-roles.ts (LIMS_PERMISSIONS) */}
