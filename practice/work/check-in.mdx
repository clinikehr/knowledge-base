---
title: Check a client in and out
description: "Record an arrival, attach the services and labs the visit is for, then check the client out and issue the visit record."
icon: /images/icons/user-check.svg
---

import { Availability } from '/snippets/availability.mdx';
import { Path } from '/snippets/path.mdx';
import { TaskHeader } from '/snippets/task-header.mdx';
import { StillStuck, AuditNote } from '/snippets/support.mdx';

<Availability editions={['clinic']} plans="Solo and Team" roles="Any staff member with access to the client record" />

Checking someone in records that they arrived, what they came for and who they are seeing. Checking them out closes the visit and lets you issue a visit record they can keep.

<Path steps={['Clients', 'A client', 'Overview', 'Check in']} />

<TaskHeader before="An existing client record" time="Under a minute" after="A visit on the client's chart, and a visit record you can download or email" />

The check-in card sits on the client chart's **Overview**. Until someone is checked in it reads **Not checked in** — "Check the client in for their visit" — with a **Check in** button beside it.

## Check someone in

<Steps>
  <Step title="Open the Check In Patient sheet">
    Select **Check in**. The sheet opens with the current time already chosen.
  </Step>
  <Step title="Confirm the arrival time">
    Under **Check-in time**, change it if they arrived earlier than you got to the screen. This is the time the visit is recorded against.
  </Step>
  <Step title="Attach the appointment, if there is one">
    Choose it under **Appointment (optional)**. Leave it on **No appointment** for a walk-in.
  </Step>
  <Step title="Say what the visit is for">
    **Visit items — services, labs, custom** takes a service, a lab test, or a custom label typed in yourself. Add as many as the visit needs.
  </Step>
  <Step title="Assign the visit">
    Pick the clinician or team member under **Assign to**, or leave it on **Unassigned** and assign later. Enter a **Location / room** if you track rooms.
  </Step>
  <Step title="Enter the reason for visit">
    Free text in **Reason for visit** — the placeholder suggests "e.g. Follow-up" — plus anything the care team should know under **Notes (optional)**.
  </Step>
  <Step title="Select Check in">
    The card on **Overview** turns green and reads **Checked in**, with the arrival time, the assigned staff member and the room.
  </Step>
</Steps>

<Note>
  The sheet refuses to save an empty visit: **Add a reason, appointment, or at least one item**. A check-in with none of the three tells the clinician nothing, so at least one is required.
</Note>

## After the check-in

Select **View** on the card to reopen the visit. From there you can:

- **Edit** — change the items, assignment, room or reason, then **Save changes**. The toast reads **Check-in updated**.
- **Check out** — close the visit. **Check out** is also on the card itself, so a routine departure takes one select.
- **Cancel check-in** — remove a visit recorded in error. The toast reads **Check-in cancelled**.

Past visits stay on the chart's **History** tab, and selecting one reopens the same sheet.

## Issue the visit record

The reopened visit has **Download PDF** and **Email patient**. Emailing confirms with **Visit record sent**.

<AuditNote action="Opening a client's check-in" />

## Check it worked

- The card on **Overview** reads **Checked in** with a live dot, and shows the time, the assigned staff member and the room.
- After checking out, the card returns to **Not checked in** and the visit is on the **History** tab.
- **Email patient** reports **Visit record sent**.

## If something goes wrong

<AccordionGroup>
  <Accordion title="Check in is greyed out">
    The chart is still loading the client record. That is deliberate — a check-in recorded before the chart knows whose it is would be attached to nobody. Give it a moment; if it never enables, reopen the chart from **Clients**.
  </Accordion>

  <Accordion title="The sheet will not save">
    You have no reason, no appointment and no visit item. Add any one of the three.
  </Accordion>

  <Accordion title="The arrival time is wrong">
    The visit is stamped with the time under **Check-in time**, not the time you saved. Reopen the visit, select **Edit** and correct it.
  </Accordion>

  <Accordion title="Checked out by mistake">
    Check them back in. There is no confirmation on check-out precisely because it is reversible this way.
  </Accordion>

  <Accordion title="The email says it was prepared rather than sent">
    Outbound email is switched off for that workspace. Use **Download PDF** and send it yourself, and see [Notification settings](/platform/settings/notifications).
  </Accordion>
</AccordionGroup>

<StillStuck />

{/* source of truth: components/clinic/clients-management/patient-checkin-sheet.tsx, components/clinic/clients-management/client-management-form.tsx, db/actions/check-ins.ts */}
